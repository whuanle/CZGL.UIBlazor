@inherits CardBase
<header class="card-header">
    @if (!(Title is null))
        @Title
@ChildContent
</header>
@code{
    /// <summary>
    /// 标题
    /// </summary>
    [Parameter]
    public RenderFragment Title { get; set; }
}