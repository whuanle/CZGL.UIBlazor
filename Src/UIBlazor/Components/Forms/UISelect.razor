@inherits SelectBase
@if (Line == null)
{
    <div class="select @_allClass" @attributes="UIAttributes"><select>@ChildContent</select></div>
}
else
{
    <div class="select is-multiple @_allClass" @attributes="UIAttributes"><select multiple size="@(Line.Value)">@ChildContent</select></div>
}